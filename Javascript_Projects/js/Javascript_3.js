function displayMood(moodObj){
    var mood = moodObj.getAttribute('data-mood');
    alert(mood);
}

// SYNTAX
// document.querySelector('p').style.backgroundColor = 'blue'; //change first p element to bg-color to blue
// document.querySelectorAll("p") // return list of p elements

 // console.log(parseFloat('32a434aadewfa')); //return integer from string (or first series of integers within a alphanumeric string)
 //                                             otherwise it returns NaN

 // TERMINOLOGY
 // (!var) -> the !exclamation is called a "bang"

 